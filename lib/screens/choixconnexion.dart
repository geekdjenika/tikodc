import 'package:flutter/material.dart';

class ChoixConnexion extends StatelessWidget {
  const ChoixConnexion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
